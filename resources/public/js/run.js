define([
	'angular',
    'angularResource',
    'less',
    'controller/teams'],
	function (angular)
	{
        angular.element(document).ready(function()
        {
			angular.bootstrap(document, ['benjals']);
		});
	});

